<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticket List</title>
    <meta name="color-scheme" content="dark">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
</head>
<body>
    <h2 class="section make-it-left make-it-white-back make-it-dark make-it-small make-it-rounded-top make-it-normal">
        Ticket List - Static View
    </h2>

    <!-- Pagination Controls -->
    <section class="section">
        <div class="container make-it-dark">
            <div class="columns is-multiline is-centered">
                <div class="column is-10">
                    <div class="make-it-dark-back is-centered grid-container-3c-by-1r" role="navigation" aria-label="pagination">
                        <a class="button section" href="?page={{prevPage}}&limit={{limit}}">Prev page</a>

                        <div class="section make-it-center make-it-rounded make-it-semi-back make-it-white">
                            Page {{currentPage}} of {{totalPages}}
                        </div>

                        <a class="button section" href="?page={{nextPage}}&limit={{limit}}">Next page</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Ticket List -->
    <section class="section">
        <div class="container">
            <div class="columns is-multiline is-centered">
                {{#if tickets.length}}
                    {{#each tickets}}
                        <div class="column is-12-mobile is-12-desktop">
                            <div class="card">
                                <div class="section">
                                    <div class="make-it-dark-back section make-it-rounded-top make-it-white">
                                        <b>Ticket ID:</b> {{this.tid}} | <b>Purchaser:</b> {{this.purchaser}} | <b>Total:</b> ${{this.amount}}
                                        <br />
                                        <small><b>Date:</b> {{this.purchase_datetime}}</small>
                                    </div>

                                    <!-- Purchased Products -->
                                    <div class="section">
                                        <h3 class="subtitle"><strong>Purchased Products</strong></h3>
                                        <table width="100%">
                                            <thead>
                                                <tr class="table make-it-semi-back make-it-white make-it-rounded">
                                                    <th>Product ID</th>
                                                    <th>Quantity</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                {{#each this.purchased}}
                                                    <tr>
                                                        <td>{{this.pid}}</td>
                                                        <td>{{this.quantity}}</td>
                                                    </tr>
                                                {{/each}}
                                            </tbody>
                                        </table>
                                    </div>

                                    <!-- Not Purchased Products -->
                                    <div class="section">
                                        <h3 class="subtitle"><strong>Not Purchased Products</strong></h3>
                                        <table width="100%">
                                            <thead>
                                                <tr class="table make-it-semi-back make-it-white make-it-rounded">
                                                    <th>Product ID</th>
                                                    <th>Quantity</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                {{#each this.notPurchased}}
                                                    <tr>
                                                        <td>{{this.pid}}</td>
                                                        <td>{{this.quantity}}</td>
                                                    </tr>
                                                {{/each}}
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {{/each}}
                {{else}}
                    <div class="has-text-centered make-it-white section">No tickets found in database.</div>
                {{/if}}
            </div>
        </div>
    </section>
</body>
</html>
